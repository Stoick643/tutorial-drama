# Local development settings
DEV_MODE=true

# LLM tutorial (Moonshot API) — required for LLM lessons 00, 04, 05
LLM_API_KEY=your-moonshot-api-key-here

# Grader backend: "docker" (default, local dev) or "subprocess" (fly.io)
# GRADER_MODE=docker
