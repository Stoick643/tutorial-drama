--dangerously-skip-permissions
  python -m venv venv
  source venv/Scripts/activate

  1. docker build -t grader-image-redis ./graders/redis
  2. uvicorn app.main:app --reload // after pip-compile
  3. prepare json file
  4. curl -X POST -H "Content-Type: application/json" --data @test_payload.json http://127.0.0.1:8000/grade

docker build -t grader-image-redis ./graders/redis

http://127.0.0.1:8000/tutorial/redis/00_setup"

grader-service
    main.py
    docker_manager.py
    schemas.py
    Dockerfile
    /graders/redis/Dockerfile